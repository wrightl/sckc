export interface PhotoSet {
  created: Date;
  id: string;
  photoCount: number;
  primaryPhotoUrl: string;
  title: string;
}

export interface Photos {
  Date: Date;
  Title: string;
  Photos: string[];
}
